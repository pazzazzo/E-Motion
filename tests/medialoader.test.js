const MediaLoader = require('../src/classes/MediaLoader');

describe('MediaLoader Functions', () => {
    const mediaLoader = new MediaLoader();
    test("test", () => {
        mediaLoader.preinit()
    })
});
